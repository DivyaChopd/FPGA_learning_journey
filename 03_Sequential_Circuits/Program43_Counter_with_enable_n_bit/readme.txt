Counter with enable: (Implemented using synchronous counter)

Here we need to add one extra input i.e. enable.
when enable = 1 then counter should work normally (like 0 1 2 3)
     enable = 0 then counter should stay the value (like 3 3 3 3)